#pragma once

#include "../HeaderFiles.h"

namespace FakeWalk{
    // inline Vector oldOrigin =Vector(0);
    // inline Vector velocity = Vector(0);
    // inline float speed = 0;
    void CreateMove(CUserCmd* cmd);
}